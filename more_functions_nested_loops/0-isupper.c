
#include "main.h"

/**
 * _isupper - checks for uppercase character.
 * @c: int to check (ASCII code of a character)
 *
 * Return: 1 if c is between 'A' and 'Z', 0 otherwise.
 */
int _isupper(int c)
{
  if (c >= 'A' && c <= 'Z')
 return (1);
 return (0);
}
